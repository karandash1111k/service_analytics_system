# Скопируйте в `.env` и подставьте свои значения.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=service_analytics
DB_USER=root
# Пусто — если у root нет пароля (частый случай локальной установки).
# Иначе укажите реальный пароль пользователя MySQL.
DB_PASSWORD=

APP_LOG_LEVEL=INFO
